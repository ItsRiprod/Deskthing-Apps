import { BluetoothPluginInterface } from "@deskthing/types"


/**
 * The Bluetooth Plugin class implementing the BluetoothPluginInterface
 * This will be able to be run within the deskthing server GUI and may automatically be run if configured once, depending on the configuration
 */
class BluetoothPlugin implements BluetoothPluginInterface {
    
    // Bluetooth config is pending implementation details
}

export default new BluetoothPlugin()